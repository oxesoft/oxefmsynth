name: Build Plugins

'on':
  push:
    branches: [ master, main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
            arch: x86_64
            name: "Linux (x86_64)"

          - os: macos-latest
            platform: macos
            arch: universal
            name: "macOS (Universal: arm64 + x86_64)"

          - os: windows-latest
            platform: windows
            arch: x64
            name: "Windows (x64)"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # -------------------------------------------------------------
      # CACHE DEPENDENCIES (.deps)
      # -------------------------------------------------------------
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: .deps
          key: deps-${{ matrix.platform }}-${{ matrix.arch }}-${{ hashFiles('Makefile.clap', 'Makefile.vst3') }}

      # -------------------------------------------------------------
      # PLATFORM-SPECIFIC TOOLCHAIN SETUP
      # -------------------------------------------------------------
      - name: Install Linux dependencies
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libx11-dev zip

      - name: Setup MSYS2 (Windows)
        if: matrix.platform == 'windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            git
            make
            zip
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-ninja

      # -------------------------------------------------------------
      # BUILD & PACKAGE
      # -------------------------------------------------------------
      - name: Build plugins (Unix)
        if: matrix.platform != 'windows'
        run: |
          make clap
          make vst3
          mkdir -p dist
          cp -R bin/oxefmsynth.clap bin/oxefmsynth.vst3 dist/

      - name: Build plugins (Windows)
        if: matrix.platform == 'windows'
        shell: msys2 {0}
        run: |
          make clap
          make vst3
          mkdir -p dist
          cp -R bin/oxefmsynth.clap bin/oxefmsynth.vst3 dist/

      # -------------------------------------------------------------
      # UPLOAD ARTIFACTS
      # -------------------------------------------------------------
      - name: Upload plugin package
        uses: actions/upload-artifact@v4
        with:
          name: oxefmsynth-${{ matrix.platform }}-${{ matrix.arch }}
          path: dist/*
          if-no-files-found: error

  # -----------------------------------------------------------------
  # RELEASE JOB (Triggers on version tags, e.g. v2.0.0)
  # -----------------------------------------------------------------
  release:
    name: Create GitHub Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Package release archives
        run: |
          cd artifacts
          for dir in */; do
            platform="${dir%/}"
            (cd "$dir" && zip -r "../${platform}.zip" .)
          done

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*.zip
          draft: true
          prerelease: false
          generate_release_notes: false

